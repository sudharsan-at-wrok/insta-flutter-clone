import 'package:flutter/material.dart';

class OlderNotification extends StatelessWidget {
  const OlderNotification({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
