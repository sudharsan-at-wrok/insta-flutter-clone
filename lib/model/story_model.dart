class StoryModel {
  final String storyUserName, storyUserProfile, storyLink;
  final bool storyIsSeen;

  StoryModel({
    required this.storyUserName,
    required this.storyUserProfile,
    required this.storyLink,
    required this.storyIsSeen,
  });
}
